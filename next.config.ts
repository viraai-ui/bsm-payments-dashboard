import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  reactStrictMode: true,
  allowedDevOrigins: ['horses-responsibilities-miss-kevin.trycloudflare.com'],
}

export default nextConfig
